// ==UserScript==
// @name         Auto-Submit (BLOQUEADO)
// @namespace    http://tampermonkey.net/
// @version      9.9.9
// @description  Script bloqueado por el autor
// @author       TuNombre
// @match        https://kolotibablo.com/*
// @grant        none
// ==/UserScript==

(function () {
    'use strict';

    // ============================================================
    // BLOQUEO INTENCIONAL
    // ============================================================
    console.error('⛔ [BLOQUEADO] Este script ha sido deshabilitado por el autor.');
    console.error('⛔ [BLOQUEADO] El código remoto ya no está disponible.');
    console.error('⛔ [BLOQUEADO] Motivo: manipulación no autorizada.');

    try {
        throw new Error('Script bloqueado: no autorizado.');
    } catch (e) {
        console.error('❌ Error intencional:', e.message);
    }

    // Opcional: mostrar aviso visible en pantalla
    /*
    const aviso = document.createElement('div');
    aviso.style.cssText = 'position:fixed;top:10px;right:10px;background:#c00;color:#fff;padding:10px;z-index:99999;font-family:sans-serif;border-radius:5px;';
    aviso.textContent = '⛔ Script bloqueado por el autor';
    document.body.appendChild(aviso);
    setTimeout(() => aviso.remove(), 5000);
    */
})();
